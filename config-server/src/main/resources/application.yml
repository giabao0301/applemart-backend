server:
  port: 8000

spring:
  profiles:
    active: dev
  application:
    name: config-server
  config:
    import: env.properties
  cloud:
    config:
      server:
        git:
          uri: https://github.com/giabao0301/config-server-applemart
          username: ${GIT_USERNAME}
          password: ${GIT_PASSWORD}
          default-label: main
          clone-on-start: true
  kafka:
    bootstrap-servers: localhost:9094

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
    fetch-registry: true
    register-with-eureka: true

management:
  tracing:
    sampling:
      probability: 1.0
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    prometheus:
      enabled: true
    health:
      show-details: always
